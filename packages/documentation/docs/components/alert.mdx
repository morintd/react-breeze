---
sidebar_position: 1
---

# Alert

An alert is used to give information.

import { Alert, Title } from 'react-breeze';

<Title as="h3">Color</Title>

<Alert className="mb-2" color="blue">
  Alert (Blue)
</Alert>
<Alert className="mb-2" color="red">
  Alert (Red)
</Alert>

```jsx
import { Alert } from 'react-breeze';

<Alert color="blue">Alert (Blue)</Alert>
<Alert color="red">Alert (Red)</Alert>
```

<Title as="h3">Visibility</Title>

An alert can be displayed and hidden thanks to a `visible` property. It takes as much space when invisible:

<Alert className="mb-2" color="blue">
  Alert (Blue)
</Alert>
<Alert className="mb-2" color="red" visible={false}>
  Alert (Red)
</Alert>

```jsx
import { Alert } from 'react-breeze';

<Alert color="blue">Alert (Blue)</Alert>
<Alert color="red" visible={false}>Alert (Red)</Alert>
```

**Properties**

Alert takes the same properties as a `div`, but can also be given:

| Name    | Type      | Default     | Required     | Description    |
| ------- | --------- | ----------- | ------------ | -------------- |
| color   | `string`  | `undefined` | **required** | Set color      |
| visible | `boolean` | `true`      |              | Display / hide |
